# 上线阻塞项修复本地回归证据

## 一、环境

| 项目 | 内容 |
|---|---|
| 执行时间 | 2026-07-06 15:52（北京时间） |
| 环境 | 本地生产构建 |
| 地址 | `http://localhost:3000`、正式模式鉴权补测端口 `http://localhost:3001` |
| 构建命令 | `npm run build` |

## 二、执行结果

| 验证点 | 结果 |
|---|---|
| 生产构建 | 通过 |
| 考试模式会话 | `/api/session` 返回 HTTP 200，`examMode=true` |
| SKU 数量小数 | 提交 `1.5` 返回 HTTP 400，issues 包含“SKU发货数量：必须为正整数” |
| 备注超长 | 提交 260 字符备注返回 HTTP 400，issues 包含“备注：长度不能超过 256 个字符” |
| 同批疑似重复外部编码 | 两行相同编码但收货信息不一致返回 HTTP 400，issues 包含“同批次外部编码重复” |
| 同单多 SKU | 两行相同编码、相同收货信息、不同 SKU 不被重复规则误拦截，进入“请先手动选择解析规则后再提交”的正常前置校验 |
| 历史大小写重复 | 先提交大写编码成功，再提交小写编码返回 HTTP 400，issues 包含“与历史批次重复” |
| 测试数据清理 | 历史大小写重复回归产生的测试运单和临时规则均已删除，删除接口返回 HTTP 200 |
| 正式模式鉴权 | 设置 `UNIVERSAL_IMPORT_REQUIRE_AUTH=true` 后，未登录访问 `/api/universal-import/templates` 返回 HTTP 401 |

## 三、关键响应摘要

```text
QTY => HTTP 400 {"issues":["第 1 行，SKU发货数量：必须为正整数"]}
NOTE => HTTP 400 {"issues":["第 1 行，备注：长度不能超过 256 个字符"]}
DUP_BAD => HTTP 400 {"issues":["第 1 行，外部编码：同批次外部编码重复，请拆分编码或确认同单多 SKU 信息一致","第 2 行，外部编码：同批次外部编码重复，请拆分编码或确认同单多 SKU 信息一致"]}
DUP_OK_NO_RULE => HTTP 400 {"error":"请先手动选择解析规则后再提交，系统不会按文件自动匹配规则。"}
HISTORY_DUP_CASE => HTTP 400 {"issues":["第 1 行，外部编码：与历史批次“FIX-HIST-...”重复"]}
AUTH_REQUIRED => HTTP 401 {"error":"请先登录后再操作万能导入。"}
```

## 四、说明

本证据为修复后的本地生产构建回归，不替代 Vercel 生产环境最终验收。正式提交或上线前建议将本轮修复部署到生产，并补跑对应生产回归截图/录屏。
