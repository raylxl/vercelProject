# 生产关键回归证据摘要

- 执行时间：2026-07-06 16:35（北京时间）
- 生产主地址：https://vercelproject-roan-theta.vercel.app
- 本次部署地址：https://vercelproject-mpytkwvgs-rays-projects-995fb56f.vercel.app
- 发布提交：`53175f4 Fix universal import launch blockers`
- 生产配置：`UNIVERSAL_IMPORT_REQUIRE_AUTH=true`
- 自动化证据 JSON：`辅助证据截图/按用例证据/production-key-regression-20260706083504.json`
- 自动化截图：`辅助证据截图/按用例证据/production-key-regression-20260706083504.png`

## 执行结果

| 检查项 | 结果 | 关键实际结果 |
|---|---|---|
| 未登录访问万能导入页面 | 通过 | `/universal-import` 跳转到登录入口 `/` |
| 未登录会话状态 | 通过 | `/api/session` 返回 HTTP 200，`authenticated=false`，`examMode=false` |
| 未登录访问规则接口 | 通过 | `/api/universal-import/templates` 返回 HTTP 401，提示“请先登录后再操作万能导入。” |
| 管理员登录 | 通过 | `admin/1234` 登录返回 HTTP 200，`authenticated=true`，`examMode=false` |
| 登录后访问规则接口 | 通过 | `/api/universal-import/templates` 返回 HTTP 200 |
| SKU 数量为 0 | 通过 | 返回 HTTP 400，提示“SKU发货数量：必须为正数”，已阻断非正数 |
| SKU 数量为 1.5 | 通过 | 返回 HTTP 400，提示“SKU发货数量：必须为正整数”，已阻断小数 |
| 备注超过 256 字符 | 通过 | 返回 HTTP 400，提示“备注：长度不能超过 256 个字符” |
| 同批疑似重复外部编码 | 通过 | 返回 HTTP 400，提示同批次外部编码重复 |

## 说明

自动化脚本原统计中 `TC-RULE-001` 按“必须为正整数”精确文案断言数量为 `0` 的场景，因此记录为 1 条失败；补充复核后确认系统对 `0` 返回“必须为正数”、对 `1.5` 返回“必须为正整数”，数量正整数业务规则已生效。该失败属于自动化断言文案口径过窄，不作为产品缺陷保留。
